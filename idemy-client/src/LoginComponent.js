import { Box, Typography } from "@material-ui/core"

const LoginComponent = (props) => {

    return (
        <Box>
            <Typography>
                LOGIN
            </Typography>
        </Box>
    )
}

export default LoginComponent;